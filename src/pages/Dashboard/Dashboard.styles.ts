import { css } from '@emotion/css';
import { Tokens } from 'styles/tokens';

export const dashboardContainer = css({
  backgroundColor: Tokens.dashboardContentBackground,
  width: '100%',
  height: '100%',
});
